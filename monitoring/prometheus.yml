global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'spring-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080'] # 'app' to nazwa serwisu w docker-compose

rule_files:
  - "/etc/prometheus/alerts.yml"
